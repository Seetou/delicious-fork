import styled from "styled-components";

const UserUIWrapper = styled.div`
  display: flex;
  justify-content: flex-end;
  align-items: center;
  width: 100%;
`;

export default UserUIWrapper;
